node_modules
.next
out
dist
build

# Tauri
src-tauri/target

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env*.local

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# Logs
*.log
npm-debug.log*

.vercel

# Tauri build artifacts
.next-tauri/
/out/

# generated (regenerated on build — do not track)
tsconfig.tsbuildinfo
next-env.d.ts

# AI作業メモ（Claude×Codexハンドオフ/執筆ノート）— コード資産ではないため追跡しない（実体は保持）
NOTE_*.md
scripts/_handoff_*.md

# ビルドログ（公開リポジトリに入れない）
.tsc-out.txt
*.log
scripts/_handoff_*.md
scripts/*.py
# 出荷前ゲートの検査（verify_*.py）は CI（check.yml）が読むので必ず追跡する（2026-09-04・verify_version_sync.py が黙って ignore されていた）
!scripts/verify_*.py
!scripts/build_tauri.js
# 検証スクリプトは追跡する（次に触る人が同じ手順で測り直せないと意味がない）
!scripts/verify_image_attachment.py
!scripts/verify_codex_route.py
__pycache__/

# Claude Code のローカル設定（個人環境・共有しない）
.claude/settings.local.json

# 社内の作業ルール（社内の絶対パスを含む）。このリポジトリは公開なのでコミットしない
HONJIN.md
GEMINI.md
