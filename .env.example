# UNICREW 環境変数サンプル。
# 実際の値は .env.local に書く（gitに含めない）。

# クラウドリレー（スマホ連携 Phase 2）用 Supabase プロジェクト。
# UNICREW 中継専用に1つ作って、その URL と anon key を入れる。
# anon key は公開しても問題ない設計（RLS無関係の Realtime broadcast のみ使用）。
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ey...
